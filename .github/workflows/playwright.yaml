name: Playwright Tests
on:
    push:
        branches: [ main ]
jobs:
    playwrighttests:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repositor
              uses: actions/checkout@v2
            - name: Say Hello
              run: echo Hello